# Web framework
fastapi==0.109.2
uvicorn[standard]==0.27.1

# Settings
pydantic-settings==2.2.1

# HTTP client (Supabase JWT verify + OpenRouter)
httpx==0.26.0

# AWS (Bedrock + S3)
boto3==1.34.34

# ML — unpinned to allow latest compatible versions per platform/Python version
torch>=2.1.0
transformers>=4.37.0
accelerate>=0.27.0

# Supabase Python client
supabase==2.3.4

# Utilities
python-multipart==0.0.9
python-dotenv==1.0.1
