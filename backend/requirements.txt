# Web framework
fastapi==0.109.2
uvicorn[standard]==0.27.1

# Settings
pydantic-settings==2.2.1

# HTTP client (Supabase JWT verify + OpenRouter); must stay <0.26 for supabase 2.3.x
httpx==0.25.2

# AWS (Bedrock + S3)
boto3==1.34.34

# ML — PINNED to the versions the full-code path was validated on.
#
# The smoke test that cleared strict state-dict load, attribution
# reconciliation and concept_share == 1.0 ran on torch 2.14.0 and
# transformers 5.17.0. Shipping a different minor version means serving
# something nobody has run the gates against, and this model's failure mode is
# silent: it returns plausible codes with wrong gates.
#
# Rollback is the previously tagged image, not a code path inside this one:
# the 50-code model has been removed entirely.
torch==2.14.0
transformers==5.17.0
accelerate>=0.27.0

# Supabase Python client
supabase==2.3.4

# Full-code concept router. pyahocorasick is NOT optional: the matcher falls
# back to a regex alternation without it, which is far slower on a 16,227-concept
# bank. The fallback is behaviourally equivalent, so its absence shows up as
# latency rather than as an error, which is the worst way to find out.
pyahocorasick>=2.0
scipy>=1.10
numpy>=1.24

# Utilities
python-multipart==0.0.9
python-dotenv==1.0.1
